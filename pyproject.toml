[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "foheart-linux"
version = "0.1.0"
description = "Linux-native USB capture foundation for FOHEART C1 routers"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.23",
    "PyYAML>=6",
    "pyusb>=1.2",
]

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.pytest.ini_options]
testpaths = ["tests"]
